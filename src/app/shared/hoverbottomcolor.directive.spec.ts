import { HoverbottomcolorDirective } from './hoverbottomcolor.directive';

describe('HoverbottomcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverbottomcolorDirective();
    expect(directive).toBeTruthy();
  });
});
